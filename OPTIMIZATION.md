# 專案優化說明與路線圖

本文件整理「專業投資人常用指標」在這個專案的落地情況：哪些已經實作、為什麼選這些、以及下一步建議接哪些資料來源。

## 已實作（本次更新）

掃描器（`stock_chip/scan.py`）在原本的法人籌碼 / 融資券 / 營收動能之外，新增以下指標，全部只用既有 SQLite 資料計算，不需要新的資料來源：

### 動能與風險（機構投資人最常用的技術面因子）

| 指標 | 欄位 | 說明 |
| --- | --- | --- |
| 區間報酬率 | `period_return_pct` | 掃描區間第一天到最後一天的漲跌幅，動能因子的基礎。 |
| RSI14 | `rsi14` | 14 日相對強弱指標（Cutler 法）。55–75 視為多頭動能，≥75 視為過熱，<30 視為超賣。需要至少 15 個交易日資料（20 日掃描才有值）。 |
| 年化波動率 | `volatility_pct` | 日報酬標準差年化（×√252）。機構用來控制部位風險；>65% 會在動能分中扣分並在理由欄標註「波動偏高」。 |
| 距區間高 / 低點 | `close_vs_high_pct` / `close_vs_low_pct` | 收盤價距離區間最高 / 最低價的百分比。接近高點（≥ -2%）代表突破慣性，是典型動能訊號。 |
| 日均成交額 | `avg_turnover_100m` | 區間平均每日成交金額（億元）。流動性門檻是法人選股的第一道過濾：新排行要求 ≥ 0.3 億／日。 |

### 估值（交易所每日揭露資料）

`valuation_score`：用本益比（≤12 強加分、≥40 扣分）、股價淨值比（≤1.5 加分、≥6 扣分）、殖利率（≥5% 強加分、≥3% 加分）計算的輕量價值傾斜分數。

### 新分數與新排行

- `momentum_score`（動能分）：區間報酬 + RSI 區間 + 距高點 + 波動率懲罰。
- `multifactor_score`（多因子綜合分）= 基礎分（法人 + 融資券 + 營收）+ 動能分 + 估值分 + 量能分 × 0.5。仿多因子模型把籌碼、基本面、動能、估值、量能放進同一個排序，**原本的基礎分（total_score）完全不變**，不影響既有報表的可比性。
- 新排行榜：
  - `multifactor_score` 多因子綜合分
  - `momentum_inst_buy` 動能 + 法人買超（區間上漲、動能分 > 0、法人買超、日均額 ≥ 0.3 億）
  - `value_dividend` 低估值 + 高殖利率（殖利率 ≥ 3%、本益比 0–20、法人未賣超、日均額 ≥ 0.3 億）

### 介面更新

- 排行頁新增「動能風險」欄位群組（區間漲跌%、RSI14、年化波動%、距區間高低%、日均額），估值群組新增估值分，核心群組新增多因子分。
- 排行下拉選單新增三個新排行。
- 個股頁「選股輔助」新增多因子分、動能分、估值分三張卡片，附上拆解說明。
- 靜態版（GitHub Pages）若尚未重新匯出、缺少新排行 JSON，會顯示明確提示而不是直接報錯。

> 在本機重新執行 `python3 -m stock_chip.scan --days 20` 與 `python3 -m stock_chip.export_static` 後，新欄位與新排行才會出現在靜態頁資料中。

## 建議的下一步（需要新資料來源）

依「投入產出比」排序：

1. **TDCC 股權分散表（每週，免費 OpenAPI）**：千張大戶持股比率與其變化是台股最有效的籌碼指標之一，和現有分點資料互補。建議新表 `shareholding_dispersion`，加入「大戶增持」排行。
2. **財報三率與 ROE（MOPS 每季）**：毛利率、營益率、淨利率趨勢 + ROE 是法人基本面篩選的核心。MOPS 的 t163sb04（綜合損益表）可免費抓。有了 EPS 也能自算更可靠的本益比。
3. **外資持股比率（TWSE MI_QFIIS，每日）**：外資「持股水位」比單日買賣超更能看出趨勢；可新增「外資持股創新高」訊號。
4. **借券賣出餘額**：與融券互補的空方指標，TWSE 每日揭露。
5. **52 週高低點**：目前距高點只看掃描區間（最長 20 日）；累積一年日線後可升級為真正的 52 週高點突破訊號（動能策略的經典定義）。
6. **Beta / 相對強弱（RS）**：大盤指數已入庫（`market_index_daily`），可計算個股對加權指數的相對強弱與 beta，把「強於大盤」做成排行。
7. **回測框架**：把每日排行前 N 名的後續報酬記錄下來，驗證各分數的預測力，再回頭調整權重。沒有回測，所有權重都只是合理猜測。

## 設計原則

- 缺資料一律輸出空值，不混入 0（沿用專案原本「缺資料 ≠ 0」的原則）。
- 新分數獨立成欄，不修改既有 `total_score` 的語意。
- 所有門檻（RSI 75、波動率 65%、日均額 0.3 億等）都集中在 `scan.py` 的對應函式內，方便調整。
