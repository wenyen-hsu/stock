# 專案優化說明與路線圖

本文件整理「專業投資人常用指標」在這個專案的落地情況：哪些已經實作、為什麼選這些、以及下一步建議接哪些資料來源。

## 已實作（本次更新）

掃描器（`stock_chip/scan.py`）在原本的法人籌碼 / 融資券 / 營收動能之外，新增以下指標，全部只用既有 SQLite 資料計算，不需要新的資料來源：

### 動能與風險（機構投資人最常用的技術面因子）

| 指標 | 欄位 | 說明 |
| --- | --- | --- |
| 區間報酬率 | `period_return_pct` | 掃描區間第一天到最後一天的漲跌幅，動能因子的基礎。 |
| RSI14 | `rsi14` | 14 日相對強弱指標（Cutler 法）。55–75 視為多頭動能，≥75 視為過熱，<30 視為超賣。需要至少 15 個交易日資料（20 日掃描才有值）。 |
| 年化波動率 | `volatility_pct` | 日報酬標準差年化（×√252）。機構用來控制部位風險；>65% 會在動能分中扣分並在理由欄標註「波動偏高」。 |
| 距區間高 / 低點 | `close_vs_high_pct` / `close_vs_low_pct` | 收盤價距離區間最高 / 最低價的百分比。接近高點（≥ -2%）代表突破慣性，是典型動能訊號。 |
| 日均成交額 | `avg_turnover_100m` | 區間平均每日成交金額（億元）。流動性門檻是法人選股的第一道過濾：新排行要求 ≥ 0.3 億／日。 |

### 估值（交易所每日揭露資料）

`valuation_score`：用本益比（≤12 強加分、≥40 扣分）、股價淨值比（≤1.5 加分、≥6 扣分）、殖利率（≥5% 強加分、≥3% 加分）計算的輕量價值傾斜分數。

### 新分數與新排行

- `momentum_score`（動能分）：區間報酬 + RSI 區間 + 距高點 + 波動率懲罰。
- `multifactor_score`（多因子綜合分）= 基礎分（法人 + 融資券 + 營收）+ 動能分 + 估值分 + 量能分 × 0.5。仿多因子模型把籌碼、基本面、動能、估值、量能放進同一個排序，**原本的基礎分（total_score）完全不變**，不影響既有報表的可比性。
- 新排行榜：
  - `multifactor_score` 多因子綜合分
  - `momentum_inst_buy` 動能 + 法人買超（區間上漲、動能分 > 0、法人買超、日均額 ≥ 0.3 億）
  - `value_dividend` 低估值 + 高殖利率（殖利率 ≥ 3%、本益比 0–20、法人未賣超、日均額 ≥ 0.3 億）

### 介面更新

- 排行頁新增「動能風險」欄位群組（區間漲跌%、RSI14、年化波動%、距區間高低%、日均額），估值群組新增估值分，核心群組新增多因子分。
- 排行下拉選單新增三個新排行。
- 個股頁「選股輔助」新增多因子分、動能分、估值分三張卡片，附上拆解說明。
- 靜態版（GitHub Pages）若尚未重新匯出、缺少新排行 JSON，會顯示明確提示而不是直接報錯。

> 在本機重新執行 `python3 -m stock_chip.scan --days 20` 與 `python3 -m stock_chip.export_static` 後，新欄位與新排行才會出現在靜態頁資料中。

## 建議的下一步（需要新資料來源）

依「投入產出比」排序：

1. **TDCC 股權分散表（每週，免費 OpenAPI）**：千張大戶持股比率與其變化是台股最有效的籌碼指標之一，和現有分點資料互補。建議新表 `shareholding_dispersion`，加入「大戶增持」排行。
2. **財報三率與 ROE（MOPS 每季）**：毛利率、營益率、淨利率趨勢 + ROE 是法人基本面篩選的核心。MOPS 的 t163sb04（綜合損益表）可免費抓。有了 EPS 也能自算更可靠的本益比。
3. **外資持股比率（TWSE MI_QFIIS，每日）**：外資「持股水位」比單日買賣超更能看出趨勢；可新增「外資持股創新高」訊號。
4. **借券賣出餘額**：與融券互補的空方指標，TWSE 每日揭露。
5. **52 週高低點**：目前距高點只看掃描區間（最長 20 日）；累積一年日線後可升級為真正的 52 週高點突破訊號（動能策略的經典定義）。
6. **Beta / 相對強弱（RS）**：大盤指數已入庫（`market_index_daily`），可計算個股對加權指數的相對強弱與 beta，把「強於大盤」做成排行。
7. **回測框架**：把每日排行前 N 名的後續報酬記錄下來，驗證各分數的預測力，再回頭調整權重。沒有回測，所有權重都只是合理猜測。

## 設計原則

- 缺資料一律輸出空值，不混入 0（沿用專案原本「缺資料 ≠ 0」的原則）。
- 新分數獨立成欄，不修改既有 `total_score` 的語意。
- 所有門檻（RSI 75、波動率 65%、日均額 0.3 億等）都集中在 `scan.py` 的對應函式內，方便調整。

## 專業分析師檢視（2026-06）

**問題：目前的資訊足夠使用者選出好股票嗎？**

結論：足夠產生高品質的「觀察名單」，不足以單獨做出買入決策。

做得到的（screener 等級，已具備）：
- 籌碼面在免費資料範圍內已是完整的：法人方向 + 連續性 + 占量、融資券、期貨多空與大盤風險乘數。
- 動能 + 量能 + 流動性過濾可以有效排除「沒人要的股票」與「已過熱的股票」。
- 營收動能涵蓋排行前 200 檔，能抓到基本面拐點的初步訊號。

做不到的（決策等級，缺口依重要性排序）：
1. **獲利品質**：沒有 EPS、ROE、毛利率/營益率/淨利率趨勢。營收成長若伴隨毛利率下滑，可能是殺價搶單；目前無法分辨。
2. **長週期動能**：視窗最長 20 日。學術與實務上最有效的動能因子是 6–12 個月相對強弱與 52 週新高；「距區間高點」只是 20 日內的弱代理。需要累積日線歷史（CI 每日累積後自然解決）。
3. **持股結構**：沒有 TDCC 千張大戶比率、外資持股水位、董監質押。法人「流量」有了，「存量」還沒有。
4. **同業比較**：個股孤立評分，沒有產業標籤與族群相對表現；台股高度族群連動，這是重要盲點。
5. **分數未經驗證**：所有權重都是合理推測，沒有回測證明預測力。在補回測之前，分數應視為「排序工具」而非「預測工具」。

使用建議（在補齊缺口前）：把排行當第一層漏斗（多因子分 → 看亮點徽章 → 個股頁確認資料品質與事件），買入決策仍需自行確認獲利品質與產業位置。

**介面檢視結論（已實作）**：排行頁新增「今日焦點」三組卡片（多因子/動能/價值 Top 3）回答「今天該看哪幾檔」；基礎分與多因子分加上分布內色條，解決「分數高低沒有參照系」的問題；理由欄改為紅綠徽章，一眼分辨利多與風險訊號。後續建議：分數百分位制（PR 值）、個股雷達圖、產業分組檢視。

## 長週期動能指標（2026-06，歷史回補後）

240 個交易日歷史回補完成後新增（`load_history_stats`）：

- `return_6m_pct` / `return_12m_pct`：6 / 12 個月報酬率（實證上最有效的動能因子）。
- `rs_6m_pct` / `rs_12m_pct`：對加權指數的相對強弱（超額報酬），正值代表強於大盤。
- `close_vs_52w_high_pct` / `close_vs_52w_low_pct`：距 52 週高低點（需 ≥120 日歷史才計算）。
- `close_vs_ma60_pct` / `close_vs_ma240_pct`：季線與年線位階。
- `long_momentum_score`（長線分）：6 月報酬 + RS + 52 週高點接近度 + 年線上下，範圍 [-12, 22]，已併入多因子綜合分。
- 新排行 `high_52w_inst_buy`：52 週新高 + 法人買超 + 流動性門檻。
- 歷史不足的個股（如新上市）欄位顯示空白、長線分為 0，不影響其他分數。

## 產業分類與族群觀察（2026-06）

- 新模組 `stock_chip/profile.py`：抓 TWSE OpenAPI 上市（t187ap03_L）與上櫃（t187ap03_O）公司基本資料，含產業別代碼對照表（24=半導體業等），存入 `stock_profiles` 表；欄位缺漏防禦性處理。
- 排行頁工具列新增「產業」下拉篩選：選任一產業即可只看該族群的排行與分數（本機與靜態版皆支援）。
- 排行表格新增「產業」欄；個股頁副標顯示產業別與主要業務（來源若未提供業務文字則只顯示產業）。
- workflow 每日同步公司基本資料（2 個 API 請求，成本極低）。
- 後續建議：產業層級的彙總視圖（族群平均多因子分、合計法人買超、上漲家數比），把「族群輪動」做成可排序的儀表板。

## 分點資料 CI 試點結論（2026-06）

實測結果：HiStock 對 GitHub Actions 機房 IP 不回應（請求懸掛，55 檔 30 分鐘零進度），CI 自動抓分點**不可行**。處置：

- CI 不再嘗試抓分點；分點維持本機個股頁手動更新。
- `run_scan` 已接回 `load_branch_leaders`：本機抓過的分點資料會自動進入排行欄位與共振分（不影響 total_score / 多因子分）。
- 若要自動化分點，可行路徑是 FinMind token（放 repo Secrets，程式已有接口）或自架有住宅 IP 的 runner。

## 族群頁與細分類（2026-06）

- 新分頁「族群」：產業熱力圖（平均區間漲跌著色，附檔數、上漲比例、合計法人買賣超、平均多因子分），點族群卡展開**第二層細分類熱力圖**與該族群完整個股表（可排序、點列進個股頁）。
- 細分類對照表 `stock_chip/sub_industries.json`：官方產業別之下的人工維護細分族群（被動元件、石英元件、PCB、晶圓代工、IC設計、AI伺服器、貨櫃航運等 27 組起始集），**直接編輯 JSON 加代號即可擴充**，一次維護長期生效。
- 排行表「產業／細分類」欄與個股頁產業標籤皆可點擊，直接跳到該族群頁。
- 公司基本資料來源修正：TPEx OpenAPI 用英文欄位名（SecuritiesCompanyCode 等），解析器同時支援中英文欄位；新增興櫃（ESB）來源。

## 覆蓋率與資料發布架構（2026-06）

- **方案 A（全市場列表覆蓋）**：search_index 改輸出全市場（~2,300 檔），搜尋、產業篩選、族群熱力圖涵蓋所有已入庫股票；非排行股票的個股頁顯示精簡版（指標完整，無 K 線/營收/每日明細），附狀態提示。完整明細維持「排行 ∪ 自選」（~430 檔 × 168KB）。
- **方案 D（site 分支發布）**：每日資料改發布到 orphan `site` 分支（git checkout --orphan + force push，歷史永遠單一 commit），main 只提交程式碼與 reports；美股新聞每小時改用 worktree 提交到 site。動機：每日重寫 ~90MB JSON 進 main 歷史已使 repo 達 111MB 且持續膨脹。
- **需要一次性手動設定**：GitHub Settings → Pages → Deploy from a branch → branch `site` / folder `/docs`。
- 全市場完整明細（推估 388MB）不適合 git/Pages，維持不做；若未來需要可評估外部物件儲存。

## 分點資料：self-hosted runner 方案上線（2026-06）

FinMind 免費等級實測無分點權限（status 400: level is register）；改採方案 B：使用者的 Mac mini 註冊為 self-hosted runner（label `branch-fetcher`），交易日 23:45 用住宅 IP 抓排行前 300 檔＋自選股的前 10 大分點，寫入共用 actions cache 的 SQLite，隨後 dispatch 雲端每日更新流程重新匯出，網站即顯示分點欄位與共振分。程式更新由 runner 每次 checkout 自動帶入，該機器無需手動同步。

## 分點資料改源 MoneyDJ，回歸雲端（2026-07）

Mac runner 實測後發現 HiStock 已把分點日報改為**登入後才顯示**（頁面回傳 `alert('請您先登入嗨投資再查詢')`，匿名只剩 2017 年空殼），住宅 IP 也救不了。改用 MoneyDJ 系統的券商網站鏡像：

- 區間主力進出：`fubon-ebrokerdj.fbs.com.tw/z/zc/zco/zco.djhtm?a=<代號>&e=<起>&f=<迄>`（元大 `jdata.yuanta.com.tw` 同路徑備援），免登入、**GitHub 雲端機房 IP 可直連**，每檔含買賣超前 15 名分點（買進/賣出/買賣超張數；無均價欄位，`avg_price` 一律空值）。
- 單一分點每日明細：`/z/zc/zco/zco0.djhtm?a=<代號>&b=<分點代號>`；含英文字母的分點代號需以 UTF-16BE 十六進位編碼（9A81 → 0039004100380031）。
- 抓取已併入每日更新流程（update-taiwan-data.yml，continue-on-error），不再需要 self-hosted runner；`fetch-branch.yml` 保留手動補抓。Mac mini runner 可自行移除（`./svc.sh uninstall`）或留作備用。
- 注意：這些券商站的 TLS 憑證缺 Subject Key Identifier，Python 3.13 預設 strict 驗證會拒絕，`branch.py` 已關閉 strict 旗標（保留一般鏈驗證）。
- 資料表 `source` 欄改記 `moneydj`；HiStock 舊碼已移除。

## 五項優化上線（2026-07）

一次補齊選股決策的三大缺口與工程韌性：

1. **TDCC 股權分散**（`tdcc.py`）：每週千張大戶/400張/散戶比率與 1 週、4 週變化，
   `dispersion_score`（±6）進多因子分，新排行「大戶增持 + 法人買超」，個股頁 26 週序列。
   無歷史回補來源，變化欄自首抓次週起有值。
2. **回測框架**（`backtest.py` + `ranking_snapshots` 表）：每日排行前 50 名快照，
   前 10 名的 5/20/60 日對 TAIEX 超額報酬與勝率；`reports/ranking_snapshots.csv`
   為 actions cache 被逐出時的持久備份（自動回灌）。累積數週後才有結論，
   在此之前資料狀態頁顯示「累積中」。
3. **季度財報**（`financials.py`）：MOPS t163sb04 整市場單季一請求；累計值差分為單季；
   三率、毛利率連升/連降、eps_ttm 與自算 pe_ttm，`fundamental_score`（±8）進多因子分。
   金融業版型缺毛利欄 → 只存淨利與 EPS。
4. **分點深化**：每日單日 top15 累積（排行前 100 檔）、主力分點連買天數、
   隔日沖分點識別（`day_traders.json` 人工名單，命中改扣分）、推估成本
   （Σ單日買超×當日均價，補回 MoneyDJ 無均價的成本訊號）。
5. **健康檢查 + 測試**（`health.py`、`tests/`、`ci.yml`）：逐來源比對最新資料日與期望，
   異常自動開/更新 GitHub Issue（label `data-health`，恢復自動關閉）；
   MoneyDJ parser 與分數邏輯的單元測試在每次 push 時跑（無網路、<1 分鐘）。

原則不變：新分數只進 `multifactor_score`，`total_score`/`base_score` 語意不動；缺資料一律空值。
